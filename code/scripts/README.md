# Scripts

One-off and operational scripts: data pulls, ad-hoc exports, scheduled retraining.

Anything that becomes part of the pipeline should move into
`code/src/sense_energy/` and be exposed through the CLI instead.
